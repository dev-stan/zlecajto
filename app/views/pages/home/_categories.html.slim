/ Constrained section (standard padding & max width)
section.py-20
    .max-w-7xl.mx-auto.px-6.lg:px-12
        .space-y-14
            .flex.items-end.justify-between
                .space-y-4
                    h2.text-6xl.font-extrabold.leading-none Kategorie
                    p.text-base.text-gray-600 Zobacz w jaki sposób oszczędzisz swój czas
                = link_to 'Zobacz wszystkie →', tasks_path, class: 'inline-flex items-center px-3 py-1.5 text-base font-medium border border-gray-300 rounded-md bg-secondary hover:bg-secondary text-primary'

            - categories = Task::CATEGORIES * 4 # temporary repetition for visual density
            ul.grid.grid-cols-3.sm:grid-cols-6.md:grid-cols-8.gap-4
                - categories.each do |cat|
                    li
                        .flex.flex-col.items-center.gap-2.p-4.border.border-amber-200.bg-amber-50/60.rounded-md.hover:bg-amber-50.transition.shadow-sm
                            / Placeholder icon logic (simple emoji rotation)
                            - icon = cat.include?('Sprząt') ? '🧹' : (cat.include?('Dev') ? '💻' : '🐝')
                            span.text-2xl = icon
                            span.text-base.font-medium.text-gray-700 = cat.gsub('Other', 'Inne')
