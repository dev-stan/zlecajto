pl:
  models:
    submission:
      sms:
        new_submission: |-
          %{first_name} właśnie zgłosił(a) się do Twojego zadania:
          "%{task_title}"

          %{note}

          Sprawdź szczegóły tutaj:
          %{url}

en:
  models:
    submission:
      sms:
        new_submission: |-
          %{first_name} just applied to your task:
          "%{task_title}"

          %{note}

          Check details here:
          %{url}